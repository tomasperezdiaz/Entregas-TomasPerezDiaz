<head>
  <style>
    button { background-color: rgb(90, 90, 90); color: white; padding: 10px
    15px; border: none; border-radius: 5px; cursor: pointer; } button:hover {
    background-color: rgb(82, 82, 82); } .user-info { display: flex;
    align-items: center; } .profile-picture { width: 40px;height: 40px;
    border-radius: 50%;overflow: hidden; margin-right: 10px; } .profile-picture
    img { width: 100%; height: 100%; object-fit: cover; }
  </style>
</head>
<header>
  <div class="header-container">
    <h1>{{title}}</h1>
    <nav>
      {{#if user}}
        <p>Bienvenido, {{user.email}}</p>
        {{#if user.image}}
          <div class="profile-picture">
            <img src="{{iser.image}}" alt="Perfil de usuario" />
          </div>
        {{/if}}
        <a href="/logout"><button>Cerrar sesion</button></a>
      {{else}}
        <a href="/login"> <button>Iniciar sesion</button></a>
        <a href="/register"><button>Registrarte</button></a>
      {{/if}}
    </nav>
  </div>
</header>